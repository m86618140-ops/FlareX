============================= FIRST CODE =============================
TRIGGER: !afk
ENABLE UNSTABLE MODE.
CODE:

$nomention
 
$c[ENABLE UNSTABLE MODE! OTHERWISE IT MAY RETURN ERRORS]
 
$if[$checkContains[$toLowercase[$getUserVar[afk]];yes]==true]
$c[You can edit the embed below adding whatever you want]
$description[
**$username[$authorID]** You're back! your afk mode has been removed
You've been afk since <t:$replaceText[$replaceText[$getUserVar[afk];no ;;1];yes ;;1]:R>
]
$color[$random[111111;999999]]
$setUserVar[afk;no]
$else
$c[You can edit the embed below adding whatever you want]
 
$description[
**$username[$authorID]** You're now on AFK mode!
Reason: $replaceText[$replaceText[%%$checkCondition[$message==]%%;%%true%%;not provided;1];%%false%%;$message;1]
]
$color[$random[111111;999999]]
 
$c[Don't edit the functions below!]
$setUserVar[afk;yes $getTimestamp]
$awaitFunc[afk;$authorID]
$c[Don't edit the functions above!]
$endif
 
 
$c[This part of the code checks if the bot can change the user's nickname, if it can't it will just not change the name into (AFK), but it will still work]
$c[Remember, bots can't change the name of server owners, they also can't change names of people with a highest role than them! that's why we add this part of the code as a limiter handler]
 
$if[$and[$rolePosition[$highestRole[$botID]]<$rolePosition[$highestRole[$authorID]];$authorID!=$serverOwner]==true]
$changeUsernameWithID[$authorID;AFK | $username[$authorID]]
$onlyIf[$checkContains[$toLowercase[$getUserVar[afk]];no]==true;]
$onlyIf[$checkUserPerms[$botID;managenicknames]==true;]
$onlyIf[$charCount[$nickname[$authorID]]<33;Your nickname is too long to change into AFK!]
$endif

============================= SECOND CODE =============================

TRIGGER: $awaitedCommand[afk;]
ENABLE UNSTABLE MODE.
CODE:

$nomention
$c[ENABLE UNSTABLE MODE]
$if[$checkContains[$toLowercase[$getUserVar[afk]];no]==true]
 
$else
$c[You can edit the embed below adding whatever you want]
$description[
**$username[$authorID]** You're back! your afk mode has been removed
You've been afk since <t:$replaceText[$replaceText[$getUserVar[afk];no ;;1];yes ;;1]:R>
]
$color[$random[111111;999999]]
 
$c[Don't edit the function below!]
$setUserVar[afk;no]
$endif
 
$c[This checks if the bot can change the user's nickname, if they can't then it won't change it, however, the afk command will still work, but it won't change the name into afk]
$if[$and[$rolePosition[$highestRole[$botID]]<$rolePosition[$highestRole[$authorID]];$authorID!=$serverOwner]==true]
$changeUsernameWithID[$authorID;$username[$authorID]]
$onlyIf[$checkUserPerms[$botID;managenicknames]==true;]
$onlyIf[$checkContains[$toLowercase[$getUserVar[afk]];yes]==true;]
$endif
 
$c[This checks if the variable "afk" exists and has typos]
$varExistError[afk;Hey! create the variable "afk" with value "no", if you have it created already and it still returning this error, then make sure it has no spaces on it, and try re-adding the variable (delete it and add it again), the variable name must be lowercase]
 
$c[ Find more exclusive codes here: https://dsc.gg/codes ]
$c[End of the second code, make sure to subscribe for more!]
$c[Berk#3506 | Bot Designer For Discord: Guide]

============================= THIRD AND LAST CODE =============================

TRIGGER: $awaitedCommand[afk;]
ENABLE UNSTABLE MODE.
CODE:

$nomention
 
$c[ENABLE UNSTABLE MODE! OTHERWISE IT MAY RETURN ERRORS]
 
$if[$checkContains[$toLowercase[$getUserVar[afk]];yes]==true]
$c[You can edit the embed below adding whatever you want]
$description[
**$username[$authorID]** You're back! your afk mode has been removed
You've been afk since <t:$replaceText[$replaceText[$getUserVar[afk];no ;;1];yes ;;1]:R>
]
$color[$random[111111;999999]]
$setUserVar[afk;no]
$else
$c[You can edit the embed below adding whatever you want]
 
$description[
**$username[$authorID]** You're now on AFK mode!
Reason: $replaceText[$replaceText[%%$checkCondition[$message==]%%;%%true%%;not provided;1];%%false%%;$message;1]
]
$color[$random[111111;999999]]
 
$c[Don't edit the functions below!]
$setUserVar[afk;yes $getTimestamp]
$awaitFunc[afk;$authorID]
$c[Don't edit the functions above!]
$endif 
$if[$and[$rolePosition[$highestRole[$botID]]<$rolePosition[$highestRole[$authorID]];$authorID!=$serverOwner]==true]
$changeUsernameWithID[$authorID;AFK | $username[$authorID]]
$onlyIf[$checkContains[$toLowercase[$getUserVar[afk]];no]==true;]
$onlyIf[$checkUserPerms[$botID;managenicknames]==true;]
$onlyIf[$charCount[$nickname[$authorID]]<33;Your nickname is too long to change into AFK!]
$endif
$varExistError[afk;Hey! create the variable "afk" with value "no", if you have it created already and it still returning this error, then make sure it has no spaces on it, and try re-adding the variable (delete it and add it again), the variable name must be lowercase]
